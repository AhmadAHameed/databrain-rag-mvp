# Backend API Configuration
VITE_API_BASE_URL=http://backend:8000

# Development settings
VITE_APP_NAME=DataBrain
VITE_APP_VERSION=0.0.1-dev

# Development/Mocking Mode
# Set to 'true' to use mock data instead of real backend API
VITE_USE_MOCKS=false

# Mock API Configuration (used when VITE_USE_MOCKS=true)
VITE_MOCK_API_DELAY=1000
